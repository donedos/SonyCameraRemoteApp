<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sleepyzzz.sonycameraremotecontrol" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- This permission is used for network positioning-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- This permission is used to access GPS location-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Used to access wifi network information, wifi information will be used for network positioning-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Obtain carrier information to support the provision of carrier-related interfaces-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- This permission is used to obtain the access permission of wifi, and the wifi information will be used for network positioning.-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Used to read the current status of the phone-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- Write extended storage, write data to the expansion card, and write offline positioning data-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Access to the network, network positioning needs to go online-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD card read permission, user writes offline positioning data-->
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="23">
        <activity android:name=".activity.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PlayerActivity"
            android:screenOrientation="landscape"
            android:launchMode="singleTask"/>
        <activity android:name="com.sleepyzzz.photo_selector.activity.PhotoPickerActivity"
                  android:configChanges="orientation|screenSize"/>
        <activity android:name="com.sleepyzzz.photo_selector.activity.TestActivity"/>

    </application>

</manifest>
